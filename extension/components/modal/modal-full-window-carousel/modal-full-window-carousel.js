/* || Component - Modal full window carousel
	 ========================================================================== */

// Initialize Modal full window carousel
export default function () {
	$('#modalFWCCarousel').slick({
		arrows: true,
		slidesToShow: 1,
		slidesToScroll: 1
	});
}
